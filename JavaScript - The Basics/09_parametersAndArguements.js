// Parameters are the variables that is defined while crating a funtion. In these variables the actual value passed to the fucntion will store in this variables called Parameters.

// Arguements are actual values that are send to the function while calling teh function.

let abc = (x) => {
    console.log(x);
}

// here 'x' is the Parameters

abc("it's an arguement");

// the Argument are the values that are passed while calling the abc fucntion